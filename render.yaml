services:
  - type: web
    name: dragonfruit-app
    env: python
    buildCommand: "pip install -r requirements.txt && npm install && npm run build"
    startCommand: "uvicorn server:app --host 0.0.0.0 --port 10000"
    plan: free
